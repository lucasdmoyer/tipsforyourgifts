// The modern publication is generated from reviewed Git content and does not
// require browser access to Firestore. Keep the database closed until a new
// feature ships with narrowly scoped rules and emulator-backed tests.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
