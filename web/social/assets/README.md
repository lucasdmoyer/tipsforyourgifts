# Approved social media assets

Each JSON record binds one public, original-or-licensed image to an exact social pack and post, with byte-level SHA-256, rights evidence, alt text, and founder identity. The file contains no media bytes or credentials.

Media approval is not content approval and is not publication. The publisher downloads the public image again and refuses to post if its bytes no longer match the approved digest.
